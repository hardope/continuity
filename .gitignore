/target
**/target
.DS_Store
*.pem
*.der

# Android — build output, Gradle cache, and the per-machine SDK path file
# (docs/android-build.md documents `local.properties` generation).
apps/android/app/build/
apps/android/.gradle/
apps/android/local.properties
# Compiled from core/continuity-ffi via cargo-ndk — regenerate per
# docs/android-build.md rather than committing prebuilt binaries.
apps/android/app/src/main/jniLibs/

# iOS — generated by XcodeGen / xcodebuild, not source (docs/ios-build.md).
apps/ios/*.xcodeproj/
apps/ios/ContinuityFFI.xcframework/
apps/ios/include/
apps/ios/DerivedData/
